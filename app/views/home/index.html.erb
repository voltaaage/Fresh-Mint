<%= react_component('Index', {heading: "Test", published: true }) %>

<%= form_for(@import) do |f| %>
  <div class="form-field">
    <%= f.label :original_file %>
    <%= f.file_field :original_file %>
  </div>
  <div class="form-field">
    <%= f.submit "Upload", class: "btn btn-success" %>
  </div>
<% end %>
